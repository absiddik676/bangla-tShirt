import React from 'react';

const Cusian = ({children}) => {
    return (
        <div>
            <h2>Cousin</h2>
            <small>{children}</small>
        </div>
    );
};

export default Cusian;